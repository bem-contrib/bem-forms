block('form').mod('message', 'popup')(
    content()(function() {
        var ctx = this.ctx;

        return [
            {
                block : 'message',
                mods : { type : 'popup' },
                mix : { block : 'form', elem : 'message' }
            },
            ctx.content
        ];
    })
);
