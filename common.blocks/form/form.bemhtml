block('form')(

    js()(true),

    tag()('form'),

    attrs()(function() {
        return {
            id : this.generateId(),
            name : this.ctx.name,
            action : this.ctx.action,
            method : this.ctx.method
        };
    }),

    def()(function() {
        applyNext({ _form : this.ctx });
    })

);
