block('form')(

    tag()('form'),

    js()(true),

    attrs()(function() {
        var ctx = this.ctx;

        return {
            action : ctx.action,
            enctype : ctx.enctype,
            method : ctx.method
        };
    })

);
