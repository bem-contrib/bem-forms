block('form').elem('field')(

    def()(function() {
        var _this = this;

        if(_this.ctx.validate) {
            applyCtx({
                block : 'validate',
                mods : _this.ctx.validate,
                target : _this.ctx.target,
                mix: [{
                    block: 'form',
                    elem: 'field'
                }],
                content : _this.ctx.content
            });
        } else {
            applyNext();
        }

    })
);
