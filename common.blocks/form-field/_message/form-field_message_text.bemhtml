block('form-field').mod('message', 'text')(
    content()(function() {
        var ctx = this.ctx;

        return [
            ctx.content,
            {
                block : 'message',
                mods : { type : 'text' },
                mix : { block : 'form-field', elem : 'message' }
            }
        ];
    })
);
