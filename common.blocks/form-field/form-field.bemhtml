block('form-field')(

    js()(true),

    def()(function() {
        var _form_field = this.ctx;
        _form_field.id = _form_field.id || this.generateId();

        return applyNext({ _form_field : _form_field });
    }),
    attrs()({ 'data-name' : this.ctx.name })

);
